<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pcmasterrace.hangman">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".HangmanActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".PlayingHangman"
            android:label="@string/title_activity_playing_hangman"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ScoreBoardActivity"
            android:label="@string/title_activity_score_board"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".GameOverActivity"
            android:label="@string/title_activity_game_over"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>